"use strict";
describe("User ACL tests to validate requests", () => {
    it("should validate user fields and create domain model", () => {
        const body = {
            name = "Prueba"
        };
    });
});
